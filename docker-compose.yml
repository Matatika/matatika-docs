version: "3"
services:
  jekyll-docs:
    image: jekyll/minimal:4.1.0
    ports:
      - 4000:4000
      - 35729:35729
    volumes:
      - ${PWD}:/srv/jekyll
      - ${PWD}/vendor/bundle:/usr/local/bundle
    command: jekyll serve --incremental --skip-initial-build
