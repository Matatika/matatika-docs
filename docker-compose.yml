version: '3'
services:
    jekyll-docs:
        image: jekyll/jekyll:3.8
        ports:
            - 4000:4000
        volumes:
            - ${PWD}:/srv/jekyll
            - ${PWD}/vendor/bundle:/usr/local/bundle
        command: jekyll serve
