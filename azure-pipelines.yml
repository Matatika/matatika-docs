# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master
- dev

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- task: DownloadReleaseArtifact@1
  inputs:
    artifactName: 'ReleaseLogs_345.zip'
    artifactStorageLocation: '$(Pipeline.Workspace)'
    unzipFile: true

- task: DownloadBuildArtifacts@0
  inputs:
    buildType: 'current'
    downloadType: 'single'
    artifactName: '/home/vsts/work/r1/a/matatika-sit/src/target.zip'
    downloadPath: '$(System.ArtifactsDirectory)'

- script: ls $(Pipeline.Workspace)
  displayName: 'Verify pipeline artifact download'