{
    "pathReplacements": [
        {
            "oldValueRegex": "(?<=profiles\\/)[a-zA-Z0-9%]*",
            "newValue": "{{profile-id}}"
        },
        {
            "oldValueRegex": "(?<=workspaces\\/)[a-z0-9-]*",
            "newValue": "{{workspace-id}}"
        },
        {
            "oldValueRegex": "(?<=datasets\\/)[a-z0-9-]*",
            "newValue": "{{dataset-id}}"
        },
        {
            "oldValueRegex": "(?<=members\\/)[a-z0-9-]*",
            "newValue": "{{member-id}}"
        },
        {
            "oldValueRegex": "(?<=invitations\\/)[a-z0-9-]*",
            "newValue": "{{invitation-id}}"
        },
        {
            "oldValueRegex": "(?<=channels\\/)[a-z0-9-]*",
            "newValue": "{{channel-id}}"
        },
        {
            "oldValueRegex": "(?<=comments\\/)[a-z0-9-]*",
            "newValue": "{{comment-id}}"
        },
        {
            "oldValueRegex": "(?<=parentId\\=)[a-z0-9-]*",
            "newValue": "{{parent-comment-id}}"
        },
        {
            "oldValueRegex": "(?<=datasources\\/)[a-z0-9-]*",
            "newValue": "{{datasource-id}}"
        },
        {
            "oldValueRegex": "(?<=subscriptions\\/)[a-z0-9-]*",
            "newValue": "{{subscription-id}}"
        },
        {
            "oldValueRegex": "(?<=notifications\\/)[a-z0-9-]*",
            "newValue": "{{notification-id}}"
        },
        {
            "oldValueRegex": "(?<=messages\\/)[a-z0-9-]*",
            "newValue": "{{message-id}}"
        },
        {
            "oldValueRegex": "(?<=pipelines\\/)[a-z0-9-]*",
            "newValue": "{{pipeline-id}}"
        },
        {
            "oldValueRegex": "(?<=jobs\\/)[a-z0-9-]*",
            "newValue": "{{job-id}}"
        },
        {
            "oldValueRegex": "(?<=tags\\/)[a-z0-9-]*",
            "newValue": "{{tag-id}}"
        },
        {
            "oldValueRegex": "(?<=email\\=)[\\w-+%\\.@]+",
            "newValue": "{{email}}"
        },
        {
            "oldValueRegex": "(?<=sequence\\=)[0-9]+",
            "newValue": "{{sequence}}"
        },
        {
            "oldValueRegex": "(?<=since\\=)(?<=since\\=)[A-Z0-9-%\\.:]+",
            "newValue": "{{since}}"
        },
        {
            "oldValueRegex": "(?<=before\\=)(?<=since\\=)[A-Z0-9-%\\.:]+",
            "newValue": "{{before}}"
        }
    ]
}